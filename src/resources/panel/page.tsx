"use client";
import LatestSupporters from "./components/latestSupporters";
import MainStats from "./components/mainStats";

import SupporterSideBar from "../../common/components/supporterSidebars";
import { useState } from "react";

export default function PanelPage({ campaigns, userId }: { campaigns: any; userId: string }) {
  const [open, setOpen] = useState(false);
  return <></>;
}
