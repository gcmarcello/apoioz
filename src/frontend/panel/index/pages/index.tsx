"use client";

import { useState } from "react";

export default function PanelPage({
  campaigns,
  userId,
}: {
  campaigns: any;
  userId: string;
}) {
  const [open, setOpen] = useState(false);
  return <></>;
}
